Create a task list and work through all of these in order. Do not skip any. Report completion of each before moving to the next.
TASK 1 — Test chat fix: Open NAHIL's chat, send 5 consecutive messages, confirm input stays unlocked after each response. Report pass or fail.
TASK 2 — Full Arabic language support: Every operator must detect when the user writes in Arabic and respond in Arabic automatically. This applies to all operators. Add language detection to chat.ts — if the last user message is Arabic, add to system prompt: 'The user is writing in Arabic. Respond in Arabic. Match their dialect if possible.' Test with NAHIL in Arabic.
TASK 3 — API section in Settings: Replace the current endpoint display with a full reference including: Operator ID, Chat endpoint URL, Authentication header example, curl example for sending a message, JavaScript fetch example, Python requests example. All using real operator values.
TASK 4 — Connectors UI: In the Integrations page replace the raw form with a clean grid of connector cards. Each card shows: service name, logo/icon, Connect button. Currently supported: Gmail, Google Calendar, Outlook, OneDrive, LinkedIn. Status shows Connected/Not connected clearly.
TASK 5 — GROW Test Mode: Before owner approves a GROW proposal, show a before/after preview. Use the 3 most semantically diverse recent messages as test prompts. Show current response vs proposed response side by side. Owner sees the difference before approving.
TASK 6 — Behavior Diff Card: When a GROW proposal is pending owner approval, show it as a clean card with: what the operator noticed, what it wants to change, why it is safe, Approve and Reject buttons. Plain human language. No jargon.
TASK 7 — Safe Mode toggle: Add Safe Mode toggle to Settings. When enabled: GROW disabled, integrations read only, nothing learned. Show a clear badge on the operator when Safe Mode is on. The operator does not know it is in Safe Mode.
*TASK 8 — Cumulative Drift cron: Add cron at 0 3 1 /3 UTC. Every 90 days compute drift score between layer2_soul_original and current layer2_soul using semanticDistance. If score > 30 flag to owner with plain language summary: 'Your operator has evolved significantly since creation. Review their current personality.' Store drift score in self_awareness_state.
TASK 9 — Final theme: Apply consistent theme across all pages. Clean, minimal, professional. Light sidebar. Dark chat area. Consistent fonts, spacing, border radius. Remove any remaining dark backgrounds from non-chat areas. Match the clean simplicity of the Base44 reference screenshots.
TASK 10 — Mobile responsive: Test and fix all pages on mobile screen size. Sidebar collapses to hamburger menu. Chat takes full screen. All buttons and inputs are touch-friendly.
TASK 11 — GitHub push: Confirm .gitignore excludes all secrets and .env files. Then commit everything with message 'OpSoul v2.4 Complete' and push to GitHub.
TASK 12 — Remove all instances of the word 'assistant' from the UI. Replace with 'operator' everywhere. Check every component file.
Work through tasks 1 to 12 in order. Do not proceed to the next task until the current one is verified. Report status after each task