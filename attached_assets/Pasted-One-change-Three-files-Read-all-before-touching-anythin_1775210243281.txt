One change. Three files. Read all before touching anything.


Reply
Context: archetype on the operators table is currently text (single value). We need to change it to text[] (array) so an Operator can have multiple archetypes — e.g. ['Advisor', 'Expert'].


Reply
STEP 1 — DB Migration


Reply
Run this SQL:

ALTER TABLE operators 
ALTER COLUMN archetype TYPE text[] 
USING ARRAY[archetype];

Reply
Confirm it ran without error.


Reply
STEP 2 — Drizzle schema


Reply
In lib/db/src/schema/operators.ts, find the archetype field and change it from:

archetype: text('archetype'),
To:

archetype: text('archetype').array(),

Reply
STEP 3 — systemPrompt.ts


Reply
In artifacts/opsoul-api/src/utils/systemPrompt.ts, find:

const archetypeFoundation = ARCHETYPE_FOUNDATIONS[operator.archetype];
if (archetypeFoundation) {
  parts.push(archetypeFoundation);
}
Replace with:

const archetypes = Array.isArray(operator.archetype) 
  ? operator.archetype 
  : operator.archetype 
    ? [operator.archetype] 
    : [];

archetypes.forEach(a => {
  const foundation = ARCHETYPE_FOUNDATIONS[a];
  if (foundation) parts.push(foundation);
});

Reply
STEP 4 — SkillsSection.tsx


Reply
In artifacts/opsoul-hub/src/components/operator/SkillsSection.tsx, update the props type:

export default function SkillsSection({ operatorId, archetype }: { 
  operatorId: string; 
  archetype: string | string[]
})

Reply
Update the filter:

const filteredSkills = showAll 
  ? platformSkills 
  : platformSkills.filter(s => {
      const archetypes = Array.isArray(archetype) ? archetype : [archetype];
      return archetypes.includes(s.archetype) || s.archetype === 'All';
    });

Reply
STEP 5 — OperatorDetail.tsx


Reply
Find:

case "skills": return <SkillsSection operatorId={id} archetype={operator?.archetype ?? 'All'} />;
Change to:

case "skills": return <SkillsSection operatorId={id} archetype={operator?.archetype ?? ['All']} />;

Reply
STEP 6 — Operator creation/edit form


Reply
Find wherever the archetype is selected during operator creation or editing (likely CreateOperatorDialog.tsx or similar). Change the single select to a multi-select checkbox group using the 6 archetype options:


Reply
Advisor, Executor, Expert, Connector, Creator, Guardian


Reply
The value should be stored as an array. Minimum 1 selection required.


Reply
Confirm: migration ran, server restarted, no TypeScript errors. Then push to GitHub.