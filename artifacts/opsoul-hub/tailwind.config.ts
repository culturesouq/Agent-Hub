/**
 * OpSoul Hub — Stitch Design System (Tailwind v4)
 *
 * In Tailwind v4, the complete theme is configured via CSS @theme in src/index.css.
 * This file documents the design-system token values and provides IDE support.
 *
 * Stitch Design System — Material Design 3 Dark palette
 *
 * Typography:
 *   font-headline  → Space Grotesk (headings, section titles, UI hero text)
 *   font-sans      → Manrope       (body copy, descriptions — Tailwind default)
 *   font-label     → Inter         (UI labels, nav items, form labels)
 *   font-mono      → Space Mono    (code blocks, API examples, technical IDs)
 *
 * Color palette (HSL):
 *   --primary               276 100% 86%   #e0b6ff  light purple
 *   --secondary             168  43% 69%   #8cd4c3  teal
 *   --background            240  29%  4%   #050508  near-black
 *   --foreground            282   7% 90%   #e6e3e8  on-surface
 *
 * MD3 surface hierarchy:
 *   --surface-dim            240 16%  6%   #0e0e12  darkest
 *   --surface                240  7%  9%   #141418  base
 *   --surface-container-lowest  240 11%  7%   #101014
 *   --surface-container-low  240  7% 11%   #1a1a1e  sidebar
 *   --surface-container      240  7% 13%   #1f1f23  cards
 *   --surface-container-high 264  6% 17%   #28272c  elevated cards
 *   --surface-container-highest 264 6% 22% #34333a  highest elevation
 *
 * Radius scale:
 *   --radius-sm   4px
 *   --radius-md   6px
 *   --radius-lg   8px   (base --radius)
 *   --radius-xl   12px
 *   --radius-2xl  16px
 *   --radius-3xl  24px
 *
 * Utility classes (src/index.css):
 *   .dot-grid            radial dot-grid background pattern
 *   .glass-panel         frosted glass container (blur + semi-transparent bg)
 *   .frosted-nav         frosted navigation bar
 *   .neon-glow-primary   purple glow box-shadow
 *   .neon-glow-secondary teal glow box-shadow
 *   .status-beacon       live teal indicator dot
 *   .headline-lg         Space Grotesk with tight tracking (display headings)
 *   .font-headline       Space Grotesk font family
 *   .font-label          Inter font family
 */
export default {};
